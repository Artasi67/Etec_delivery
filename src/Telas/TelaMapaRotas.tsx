import { SafeAreaView } from "react-native-safe-area-context";
import { StyleSheet, Text } from "react-native"

const TelaMapaRotas = () => {
    return(
        <SafeAreaView>
            <Text>Tela de Mapa de Rotas</Text>
        </SafeAreaView>
    )
}

const estilos = StyleSheet.create({

})

export default TelaMapaRotas